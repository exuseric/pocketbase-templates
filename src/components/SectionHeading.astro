---
const {sectionTitle, excerpt, textColor} = Astro.props
---

<header class={`${textColor} mb-4 border-b-4 border-b-current w-max`}>
    <h2 class={`text-2xl sm:text-4xl font-extrabold leading-3 text-current`}>
        {sectionTitle}
    </h2>
    <p class="mb-6 max-w-prose text-current">{excerpt}</p>
</header>